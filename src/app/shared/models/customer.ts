export interface ICustomer {
    accountId: string;
    customerId: string;
    currency: string;
    accountName: string;
    accountNumber: string;
    sortCode: string;
    balance: number;
    status: string;
}
